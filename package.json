{
  "name": "flowbot",
  "version": "1.0.0",
  "private": true,
  "type": "commonjs",
  "engines": {
    "node": "22.x"
  },
  "scripts": {
    "build": "npm ci --prefix backend && npm ci --include=dev --prefix frontend && npm run build --prefix frontend",
    "start": "npm start --prefix backend"
  }
}
